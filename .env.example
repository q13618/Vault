# 在 Vercel 项目里创建 Blob 存储并关联后，平台会自动注入这个变量。
# 本地开发时执行 `vercel env pull` 拉取，或手动粘贴。
BLOB_READ_WRITE_TOKEN=

# 可选：保护定时清理接口。在 Vercel 里设置后，Cron 会自动带上 Bearer 头。
CRON_SECRET=

# 可选：匿名用户单文件上限（MB），默认 200。
VAULT_ANON_MAX_MB=200
