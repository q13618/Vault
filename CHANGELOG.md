# 更新日志

## v0.1.0 — 2026-09-15

首个可用版本。已在生产环境（<https://f.airppp.com>）验证通过。

### 临时文件中转

- 拖入文件即得短链接 `https://f.airppp.com/d/<10 位短码>`，够短，微信、短信里发得出去
- 浏览器**直传对象存储**，不经过 Serverless 函数转发，因此不受请求体大小限制；
  超过 8 MB 自动走分片上传，几个 G 的视频也传得动
- 保留时长可选 1 小时 / 6 小时 / 1 天 / 3 天 / 7 天，默认 1 天；匿名单文件上限 200 MB
- 额度由对象存储服务端强制执行：改前端代码绕不过大小限制，改链接也延长不了有效期
- 到期后下载入口立即拒绝访问，每小时的定时任务再把对象从存储里彻底删除
- 上传者可凭撤回口令提前删除；口令只以 SHA-256 摘要落盘，别人即使拿到直链也删不掉
- 下载页支持图片 / 音视频预览、到期倒计时；分享链接不被搜索引擎收录
- 支持二维码，方便从电脑分享到手机

### 网页转 PDF

- 贴一个网址即可渲染成 PDF，同样给一条到期作废的短链接
- 渲染由 Cloudflare Browser Rendering 承担。选托管而非在函数里自建 Chromium，
  主要原因是中文字体：`@sparticuz/chromium` 只内置 Open Sans，CJK 会渲染成豆腐块，
  而且坏掉的字体会被嵌进 PDF 成为永久产物
- 产物走**与上传文件完全相同的对象路径约定**，所以到期作废、提前撤回、短链分享、
  定时清理全部自动复用，这条路径没有引入任何新状态
- 文件名由网址推导且保留中文：`https://www.zhihu.com/question/12345`
  → `zhihu.com-question-12345.pdf`；查询串不进文件名
- 地址校验只放行 http/https，拒绝回环、内网、链路本地（含云元数据端点
  `169.254.169.254`）以及带凭据的地址；所有拒绝都发生在发起网络请求之前

### 架构

- **零数据库**：到期时间、原始文件名、撤回凭据全部编码在对象路径里
  （`f/<id>/<到期秒>/<随机段>/<撤回哈希>/<文件名>`），没有需要备份的状态
- 渲染实现收在 `lib/render.ts` 的 `PageRenderer` 接口后面，换实现只改一个文件
- 套餐额度集中在 `lib/plans.ts`；注册与付费档位的额度已定义好并在首页展示为
  「即将开放」，待账号体系与支付接入后让 `resolvePlan()` 返回对应套餐即可生效
- 分享链接使用站点规范域名，绑定自定义域名后自动切换，无需改代码

### 已知限制

- 有登录墙或人机验证的页面渲染不出来 —— Cloudflare Browser Rendering 设计上尊重
  bot 防护，不做伪装。这类失败会明确报错，而不是给出一张空白 PDF
- 清理任务每小时跑一次，过期文件最多在存储里多留一小时；但下载入口在到期那一刻
  就已拒绝访问
- **没有做用量限流。** 上传只消耗存储，而网页转 PDF 消耗按时长计费的浏览器额度，
  公开运营前建议在 Vercel 侧加上 WAF / 速率限制
- 预览图片、音视频时浏览器最终会拿到对象直链，熟悉开发者工具的收件人能看到路径里的
  随机段。这只影响「绕过到期判断直连文件」，撤回权限由独立的哈希把守，不受影响
